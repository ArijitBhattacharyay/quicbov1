{
  "name": "quicbo-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && python3 -m uvicorn main:app --reload",
    "build:frontend": "cd frontend && npm install && npm run build"
  }
}
